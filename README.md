## Task
-create a machine learning model that 
predicts a students exam score based on  given factors

# imports
pandas to work with the data set  
matplotlib to visualize the data set    
sklearn for the linearregression and to split the data and validate the model
# steps in the model creation process
1-load the csv file and run to see the data set  
2-get the shape to see the number of rows and columns  
3-get the number of missing values  
4-get a describtion and additional infomation about the data set  
5-declear a variable for the columns that I regard relevant  
6-visualize to see what columns that best fit the linear regression model  
7-define X(features mattrix) and y (target label)  
8-creating the model and fiting the training data  
9-validating the training and test results  
10-visualizing the actual vs the predicted data  
11-creating a new data set to test and perform prediction 
## tools I used
python in google colab


